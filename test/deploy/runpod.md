# Set of scripts for testing on runpod instances

So the runpod instances are quite cheap and hence a good option for testing out these images

There is a runpod python api https://docs.runpod.io/sdks/python/apis